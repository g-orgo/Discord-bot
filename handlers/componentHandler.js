/**
 * Handles MESSAGE_COMPONENT interactions.
 * @param {import('express').Request} req
 * @param {import('express').Response} res
 */
export async function handleComponent(_req, _res) {
  return;
}
