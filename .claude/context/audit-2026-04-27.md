# Context - Security hardening for Discord history sync

Date: 2026-04-27

## Summary
Updated `api/api.js` to remove hardcoded `DISCORD_BOT_SECRET` fallback and added an explicit guard to skip history sync when the secret is not configured.

## Files modified
- `api/api.js`: `DISCORD_BOT_SECRET` now sourced only from env; added early return with warning log when missing.

## Decisions made
- Kept local defaults for service URLs (`LLM_URL`, `AUTH_URL`) aligned with existing project conventions.
- Removed secret default to avoid insecure implicit credential behavior.

## Known issues / next steps
- Ensure deployment environments set `DISCORD_BOT_SECRET` where Discord history sync is expected.
